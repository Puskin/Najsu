<ul class="unstyled" id="users_list">
	<% @users.each do |user| %>
		<li class="user_<%=user.id%>">
		<%= user.name %>
		<div id="follow_form" class="pull-right">
			<%= render :partial => "users/followForm", :locals => { :user => user } %>
		</div>
		</li>
	<% end %>
</ul>