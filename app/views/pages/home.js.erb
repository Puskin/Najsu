<% if params[:page] %>
	<% if params[:page].to_i <= @movies.total_pages %>
		$("#moviesList").append("<%= escape_javascript( render @movies ) %>");       
		$("#moviesList").masonry( 'reload' );       
		$("#paginaDomina").html("<%= escape_javascript( will_paginate @movies, :page_links => false, :next_label => 'Więcej!' ) %>");       
	  $('.pagination a').attr('data-remote', 'true');
	  $('.previous_page').remove()
	<% end %>
<% else %>
	$("#moviesList").html("<%= escape_javascript( render @movies ) %>");       
	$("#information").html("<%= escape_javascript( render :partial => 'pages/homeNotice' ) %>");       
	$("#paginaDomina").html("<%= escape_javascript( will_paginate @movies, :page_links => false, :next_label => 'Więcej!' ) %>");       
	$("#moviesList").masonry( 'reload' );       
		$('[rel="tooltip"]').tooltip();
	 $('.pagination a').attr('data-remote', 'true');
	 $('.previous_page').remove()
	 heightCheck();
<% end %>


/*
$("#moviesList").delay(2000)
    .queue(function(n) {
        $(this).html("<%= escape_javascript( render @movies ) %>");       
        n();
        $("#moviesList").masonry( 'reload' );
    })
*/
