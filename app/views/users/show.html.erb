<% provide(:title, @user.name) %>

<p>
  <b>Name:</b>
  <%= @user.name %>
</p>

<p>
  <b>Email:</b>
  <%= @user.email %>
</p>

<p>
	<% @user.authentications.each do |auth| %>
		<%= auth.provider %></br>
		<%= auth.uid %></br>
		<hr>
	<% end %>
</p>

<%= render :partial => "users/followForm", :locals => { :user => @user } %>

<strong>Obserwowani:</strong><br/>
<% @user.followed_users.each do |followed| %>
	<%= followed.name %>
<% end %><br/>

<strong>Obserwujący:</strong><br/>
<% @user.followers.each do |follower| %>
	<%= follower.name %>
<% end %><br/>


<!--
<div id="categories" class="row-fluid library">
	<div class="avatar">
		<div class="mask"></div>
		<%= image_tag(gravatar_url_for(@user),:size => '82x82') %>
	</div>
</div>

<div class="row-fluid libary-navi">
	<div class="btn-group">
		<%= link_to "Twoje", user_path(:show => "yours"), :id => "yours", :class => "btn active", :remote => true %>
		<%= link_to "Lubiane", user_path(:show => "liked"), :id => "liked", :class => "btn", :remote => true %>
		<%= link_to "Komenty", user_path(:show => "commented"), :id => "commented", :class => "btn", :remote => true %>
	</div>
</div>


<div class="row-fluid">

	<div id="moviesList" class="library">
		<%= render :partial => "movies/repost", :collection => @movies %>
	</div>
</div>
-->