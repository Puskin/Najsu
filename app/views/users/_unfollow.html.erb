<%= form_for(current_user.relationships.find_by_followed_id(user), html: { method: :delete }, :remote => true) do |f| %>
  <%= f.submit "Nie obserwuj", :class => "btn btn-mini", :disable_with => "Loading..." %>
<% end %>