<ul class="unstyled" id="activities">
	<%= render :partial => "activities/listing", :locals => { :activities => @activities } %>
</ul>

<%= will_paginate @activities, :page_links => false, :next_label => 'Load more!' %>
