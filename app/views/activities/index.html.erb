<ul class="unstyled" id="activities">
	<% last_activity_user = @activities.last.user_id %>
	<% last_activity_date = @activities.last.created_at %>

	<% @activities.each do |activity| %>
	
			<% unless activity.created_at.strftime("%Y%m%d") == last_activity_date.strftime("%Y%m%d") %>
				<% unless @activities.first == activity %>
					<li class="date">
						<p><%= activity.created_at.strftime("%e %B") %></p>
					</li>				
				<% end %>
			<% end %>

	<% last_activity_date = activity.created_at %>	

	<% unless activity.user_id == current_user.id && activity.recipient_id == current_user.id %> 
 	<li <%= 'class=first' if last_activity_user != activity.user_id %>>
 				
 				<% unless @activities.first != activity %>
 					<div class="avatar">
						<div class="mask" rel='tooltipBottom' title ="<%= activity.user.name %>"></div>
						<%= image_tag(gravatar_url_for(activity.user), :size => '64x64') %>
					</div>					
				<% end %>

				<!-- save current activity user -->
				<% last_activity_user = activity.user_id %>


				<div class="content">
					<% if activity.resource != 3 %>
						<%= image_tag(activity.data[:movie_thumbnail]) %>
					<% end %>

						<%= activity.user.name %> <%= activity_action(activity.resource) %> 
						<%= "'#{activity.data[:movie_title]}'" if activity.resource != 3 %> 
						<%= time_ago_in_words(activity.created_at) %> temu.<br/>

					<% if activity.resource == 2 %>
						"<i><%= activity.data[:comment_content] %></i>"
					<% end %>
				</div>
	</li>
	<% end %>


 <% end %>
</ul>